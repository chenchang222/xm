<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.PostCommentMapper">

    <insert id="insert" parameterType="org.example.entity.PostComment" useGeneratedKeys="true" keyProperty="id">
        insert into post_comment
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="postId != null">post_id,</if>
            <if test="content != null">content,</if>
            <if test="commenterId != null">commenter_id,</if>
            <if test="commenterName != null">commenter_name,</if>
            <if test="parentId != null">parent_id,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="postId != null">#{postId},</if>
            <if test="content != null">#{content},</if>
            <if test="commenterId != null">#{commenterId},</if>
            <if test="commenterName != null">#{commenterName},</if>
            <if test="parentId != null">#{parentId},</if>
        </trim>
    </insert>
</mapper> 