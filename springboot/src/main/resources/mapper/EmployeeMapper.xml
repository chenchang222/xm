<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

    <mapper namespace="org.example.mapper.EmployeeMapper">

    <select id="selectAll" resultType="org.example.entity.Employee" >
        select * from employee
        <where>
            <if test="name!=null">name like concat('%',#{name},'%')</if>
        </where>
    </select>

    <insert id="insert" parameterType="org.example.entity.Employee">
        insert into employee(username,password,role,name,sex,no,age,description)
        values(#{username},#{password},#{role},#{name},#{sex},#{no},#{age},#{description})
    </insert>

    <update id="updateById" parameterType="org.example.entity.Employee">
        update employee
        set username=#{username},password=#{password},role=#{role},name=#{name},sex=#{sex},no=#{no},age=#{age},description=#{description}
        where id=#{id}
    </update>

    <!-- 批量删除 -->
    <delete id="deleteByIds">
        DELETE FROM employee
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
    </mapper>